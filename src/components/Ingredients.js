import React from "react"
import { Container, Row, Col, Image } from 'react-bootstrap'

const Ingredients = ({ ingredient }) => {
    const { label } = ingredient.food;

    return(
        <div>

        </div>
    )
}

export default Ingredients